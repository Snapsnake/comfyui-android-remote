# Source-node navigation

When an API-prompt input is represented as a connection such as `["320_327", 0]`, the local field belongs to the upstream node rather than the receiving node. The mobile client must preserve the connection and navigate to that source node for editing. It must not replace the connection with a local value.
